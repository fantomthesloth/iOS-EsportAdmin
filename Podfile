# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'EsportAdmin' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EsportAdmin
  pod 'SwiftyJSON'
  pod ‘Alamofire’
  pod 'SDWebImage', '~> 4.0'
  pod 'SideMenu'
  pod 'SnapKit', '~> 4.0'

end
